# Context Snapshots

System utility directory for save-memory snapshots.
Not governed by PARA — this is operational state.

When you say "save memory," a context snapshot is written
here as `latest.md`. Previous snapshots are archived with
date-based filenames.
